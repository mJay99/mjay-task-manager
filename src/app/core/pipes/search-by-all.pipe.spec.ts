import { SearchByAllPipe } from './search-by-all.pipe';

describe('SearchByAllPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByAllPipe();
    expect(pipe).toBeTruthy();
  });
});
